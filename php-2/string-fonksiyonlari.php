<?php

/*
 * strlen() // String değerler deki toplam kaç harf olduğunu gösterir.
 * strstr() // String ifade de verdiğimiz ifadeyi aramaya yardımcı olur.
 * strpos() // String ifadeler de belirttiğin değerin hangi değerde olduğunu rakamla belirtir.
 * ucwords() // String ifadeler de her kelimenin baş harfini büyütmeye yarar.
 * ucfirst() // String ifadeler de cümlenin ilk harfini büyük yazar.
 * strtolower() // String ifadeler de bütün kelimeleri küçük yazdırır.
 * strtoupper() // String ifadeler de bütün kelimeleri büyük yazdırır.
 * trim() // String ifadeler de boşlukları kaldırmak için kullanılır.
 * ltrim() // String ifadeler de sol boşlukları kaldırmak için kullanılır.
 * rtrim() // String ifadeler de sağ boşlukları kaldırmak için kullanılır.
 * substr() // String idadeler de kelimeyi bölmek için kullanılır.
 * str_replace() // String ifadeler kelimeyi değiştirmek için kullanılır, Kullanımı str-str-değer. ilk değer çıkarılacak kelimedir. Birden fazla değişiklik için diziye çevrilmelidir.
 * str_repeat() // String bir ifadenin kaç kez tekrar edilmesi gerek onu belirtmek için kullanılır.
 */

      $name = 'alparslan'. ' ' .'kerim';
      $subnames = 'küçük Emrah' . ' ' . 'küçük Alp';
//  echo  '<h1>' . strlen($name) . '</h1>';
//  echo  '<h1>' . strstr($name, 'A') . '</h1>' ;
//  $smallWords = ucwords($subnames);
//  echo $smallWords;
//  $upperFirstWords = ucfirst($name);
//  echo $upperFirstWords;
//  $nameToLower = strtolower($subnames);
//  echo  $nameToLower;
//  $nameToUpper = strtoupper($name);
//  echo  $nameToUpper;

?>
