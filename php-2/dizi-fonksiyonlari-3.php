<?php
/*
 * array_values() // Dizi de ki değerlerin key sıralamasını düzenler. Örnek olarak tekrarlanan bir dizi çıkarıldığın da key değerini atlar bu yüzden values kullanılır sıra bozulmaması için.
 * array_push() // Dizilere yeni değer eklemek için kullanılır.
 * array_unshift() // Dizinin başına değer ekler.
 * array_keys() // Dizinin keylerini listelemeye yarar.
 * current() // Dizinin ilk elemanını bulur.
 * end() // Dizinin son elemanını bulur.
 * next() // Dizinin 2. elemanını bulur.
 * prev() // Dizideki gösterilen değerin önceki değerini bulur.
 * reset() // Diziyi resetler başa getirir.
 * extract() // Dizi de ki anahtarları bir değişken gibi kullanılmaya yarar.
 * asort() // Dizi deki değerleri küçükten büyüğe doğru sıralar.
 * arsort() // Dizi deki değerleri büyükten küçüğe doğru sıralar.
 * ksort() // Dizi de ki keylere göre küçükten büyüğe doğru sıralar.
 * krsort() // Dizi de ki keylere göre büyükten küçüğe doğru sıralar.
 */

$arr = [
    'name'=> 'Alparslan',
    'surname'=> 'Akbaş'
];


?>
